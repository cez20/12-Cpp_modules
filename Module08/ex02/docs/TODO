

Stack:
    - LIFO(Last In First Out);
    - Elements are INSERTED and EXTRACTED only from one end of the container
    - push_back: push an element on top of the stack
    - pop: removes the element on top of the stack
    - Functions that are part of std::stack
        -empty
        - size
        - back
        - push_back
        - pop_back


Instructions:

[] Rendre le std::stack iterable, car pas defaut ce container n'est pas iterable
    [] Implementez une classe MutantStack. 
        [] Elle sera implementee en termes de la std::stack. 
        [] Elle offrira toutes ses fonctions membres 
        [] Elle offrira en plus des iterateurs ...ce qui nest pas le cas dans la std::stack de base 

    [] Executez lexempple donne dans les instructions

    [] Remplace tous les MutantStack par std::list devrait donner le meme resultat 
    [] On peut tester avec un autre containers. 